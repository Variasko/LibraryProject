<Page x:Class="LibraryDesktop.Views.Pages.BooksPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LibraryDesktop.Views.Pages"
      xmlns:vm="clr-namespace:LibraryDesktop.ViewModels.PagesViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="BooksPage">
    <d:Page.DataContext>
        <vm:BooksPageViewModel/>
    </d:Page.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <WrapPanel Orientation="Horizontal"
                       Margin="20 10">
                <ItemsControl>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Tag="{Binding Id}">
                                <StackPanel>
                                    <TextBlock Style="{DynamicResource SubtitleTextBlockStyle}">
                                        <Run />
                                    </TextBlock>
                                    <TextBlock>
                                        <Run />
                                        <Run />
                                        <Run />
                                    </TextBlock>
                                    <TextBlock>
                                        <Run />
                                        <Run Text="-"/>
                                        <Run />
                                    </TextBlock>
                                    <TextBlock>
                                        <Run Text=""/>
                                    </TextBlock>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </WrapPanel>
        </ScrollViewer>

    </Grid>
</Page>
